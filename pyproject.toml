[project]
name = "hellas-firewatch"
version = "0.1.0"
requires-python = ">=3.10"
description = "Community verification for wildfire detections (Python MVP)"
readme = "README.md"

[tool.ruff]
line-length = 100
